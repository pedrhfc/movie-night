import { StyleService } from "@ui-kitten/components";
import { container } from "styles/variables";

const navigateScreenStyle = StyleService.create({
  container,
});

export default navigateScreenStyle;
