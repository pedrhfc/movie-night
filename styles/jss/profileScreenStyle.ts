import { StyleService } from "@ui-kitten/components";
import { container } from "styles/variables";

const profileScreenStyle = StyleService.create({
  container,
});

export default profileScreenStyle;
